allprojects {
    ext {
        appName    = 'NobelEngine'
        gdxVersion = '1.12.1'
    }

    repositories {
        mavenCentral()
        maven { url 'https://oss.sonatype.org/content/repositories/releases/' }
    }
}
